<resources>

    <!-- 定义按钮样式 -->
    <style name="CustomButtonStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">18sp</item>
        <item name="android:background">@drawable/ic_launcher_background</item>
        <item name="android:padding">16dp</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <!-- 日历文字样式，让日期数字更大、更适应空间 -->
    <style name="CalendarDateText" parent="@android:style/TextAppearance.Medium">
        <item name="android:textSize">20sp</item>
    </style>

    <style name="CalendarWeekText" parent="@android:style/TextAppearance.Small">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="CalendarWeekHeaderCell">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">#FF666666</item>
    </style>

</resources>